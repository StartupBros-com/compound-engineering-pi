---
description: Legacy feature video prompt retained for compatibility
argument-hint: "[PR number or current feature context]"
---

# Legacy feature video prompt

Compound Engineering 3.13.1 no longer bundles the `ce-demo-reel` workflow.
Do not call `ce-demo-reel` or any removed video skill.

If the user still wants a walkthrough, provide a concise manual demo checklist
or use currently available browser/video tools in the active Pi environment.
Summarize any PR follow-up the user should do manually.

<context>#$ARGUMENTS</context>
